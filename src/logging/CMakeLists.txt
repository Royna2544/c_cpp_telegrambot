if(WIN32)
  tgbot_library(
    NAME
    LogInit
    SRCS
    SpdlogInit.cpp
    LIBS
    spdlog::spdlog
    NO_HIDE
    STATIC)
else()
  tgbot_library(
    NAME
    LogInit
    SRCS
    SpdlogInit.cpp
    LIBS
    spdlog::spdlog
    NO_HIDE)
endif()

tgbot_exe(
  NAME
  logcat
  SRCS
  LoggingClient.cpp
  LIBS
  Socket
  Utils
  sighandler
  RELATION
  Socket)
