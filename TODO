- Handle forked process in forkandrun when the main process exits due to network.
- Use jthread for ThreadManager
- SQLiteDatabase should return something else when database backend has failed, instead of bool